.vercel/
node_modules/
dist/
.next/
.source/
next-env.d.ts
*.tsbuildinfo
*.log
.DS_Store
.env*
.openklip-worktrees/
.openklip/
tmp/

# source media: keep large/private footage out of the repo
*.mp4
*.MP4
*.mov
*.MOV
*.aiff
*.wav
*.f32
.gstack/
.qa/
tools/vision-focus
.vercel

# CRAFT-6186: acceptance corpus is generated fresh by
# scripts/acceptance-corpus.ts on every run (including manifest.json, which
# embeds an absolute path + generation timestamp); never commit it.
/fixtures/acceptance/

# CRAFT-6187 Stage B: generated by scripts/prepare-desktop-bundle.ts on every
# packaging pass. Contains a full copy of node_modules, the built .next
# output, and (once Tauri's externalBin resource wiring lands) a 60MB+ Bun
# binary; never commit it.
/src-tauri/resources/app/
