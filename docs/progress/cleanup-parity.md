# Cleanup parity (Cutback "Cut unnecessary parts" card)

Branch: `claude/feature-availability-check-f8b1a8` · Decided 2026-07-12 (20-question interview, all recommendations accepted)

## Spec (locked)

- Engine-level `category` on every cleanup candidate: `hesitation | hedging | repeat | dead-air`; `kind` stays for back-compat. False starts fold into `repeat`.
- New deterministic repeat detector: immediate n-gram repeats (n=1–3) among kept words, keep the last take, ≤0.6s adjacency, review risk. LLM pass (subscription CLIs) remains the false-start/contextual-"like" detector.
- Persisted `project.cuts.cleanup` config: `minSec` (default 0.7), `keepPadSec` (default 0.15), `categories` (hesitation on; hedging/repeat off).
- New registry actions on cli/gui/mcp: `cleanup-config` (merge settings) and `cleanup-apply` (`mode: safe|enabled`, returns `{wordIds, deadAirSpanIds}` for undo). `--apply-safe` semantics frozen; `--apply-enabled` = enabled categories at any risk + ALL dead-air ≥ minSec (slider-is-consent).
- GUI: dedicated Cleanup tab (category checkbox cards with counts, bulk apply, undo-last), Remove-silence card with waveform strips + threshold controls, hover/pending highlight in transcript, explicit "AI pass" button.
- Peaks: on-demand `GET /api/projects/:slug/peaks?fromSec&toSec&buckets` computed from `working/audio16k.f32`; no persisted artifacts.
- Ledger: evolve `cleanup-review` entry + exactly one new feature entry (51→52). Docs via docs-sync after slices.

## Slices

1. Engine: categories, repeat detector, cleanup config, cleanup-config/cleanup-apply, CLI `--apply-enabled`.
2. GUI: Cleanup tab, category cards, bulk apply, undo-last.
3. Silence card: peaks route + waveform + threshold controls (+ new feature entry).
4. AI-pass button, pending highlight, docs sync, end-to-end demo QA.

## Final quality chain (2026-07-12)

- Claude subagent red-team + docs-sync hit the session limit; rerouted per TStack lanes: codex (GPT-5.5) second-opinion adversarial review, docs sync done by the orchestrator directly.
- codex verdict BLOCK with 4 findings → resolved: (1) BLOCKER stale AI-pass results merging into a different project after slug switch (word ids reused across projects → cross-project cuts) — fixed via slug+requestId flight guard in new use-cleanup-ai-pass hook, race-tested; (2) MAJOR peaks route unbounded window (~691MB alloc on 3h file) — 120s clamped-window cap before any PCM read; (3) MAJOR silences route cold inline analysis on long footage — accepted as consistent with the repo's compute-and-cache pattern, documented in TODO known limitations; (4) MINOR loose param validation — raw values now 400 on negative/reversed/non-positive-int before clamping. Live-verified via curl: valid window 200 with real peaks, -10 fromSec 400, buckets=-5 400, silences hydrating.
- Docs synced to measured truth: README (2022→ now 2031 tests noted at ship, 92 MCP tools, categorized cleanup bullets), TODO status/limitations, CHANGELOG Unreleased section. README-parity tests green.
- Final gates: 2031 tests / 0 fail / 5 env-conditional skips; tsc clean; ultracite clean (774 files); Next production build green.

## Verification (browser round 3, 2026-07-12)

- Undo integrity fixed and verified at the filesystem: Remove all silences → 4 spans in project.json → Undo last cleanup (4) → 0 spans, 0 deleted words.
- AI pass end-to-end live: "Find false starts & mistakes (AI)" → agent CLI runs server-side → correct "No false starts or mistakes found" on the demo (deterministic detector already owns every planted repeat; dedupe holds).
- Pending hover highlight: hovering a candidate row dashes-underlines its words in the transcript (distinct from applied-cut strikethrough).
- Dark mode: waveform, cards, and buttons all theme via CSS variables; no artifacts.
- Final gates (orchestrator-run): 2022 tests / 0 fail / 5 env-conditional skips; tsc clean; ultracite clean repo-wide (773 files); Next production build green. features --json = 52.

## Execution log

- 2026-07-12 · Wave A launched: grok lane (composer 2.5) session 1 = slice 1 engine; session 2 = peaks engine+route (slice 3 engine half, disjoint files). Orchestrator (Claude) reviews and judges every lane diff; gates = bun test / typecheck / lint / build per wave.
- 2026-07-12 · Wave A landed and orchestrator-reviewed. Gates green under orchestrator's own runs: bun test 1962 pass / 5 skip / 0 fail (1967 total, +63 vs 1904 baseline; +2 skips are environment-conditional, none authored), typecheck and build clean. Demo project `parity-demo` (synthesized disfluent speech, 56 verbatim Whisper words + 4 planted silences) reports exactly per design: 4 hesitation, 2 hedging, 3 repeat, 4 dead-air (1.3s/2.1s safe; 1.0s/0.9s review).
- Fix list from orchestrator review (fix round after reviewer reports): (1) provenance.ts wordIdsForAction lacks "cleanup-apply" case → word attribution lost (also regresses --apply-safe provenance vs legacy cut-action logging); (2) MAX_NGRAM 3→6 so long false starts ("I want to show you ×2") are caught deterministically; (3) cleanup-apply reads silences from cache synchronously — cold project via MCP silently degrades to filler-only (CLI safe: report warms cache); (4) repeat×hedging overlap can double-count estSavedSec (assess).
- 2026-07-12 · Wave B launched: grok session B1 = slice 2 Cleanup tab GUI; grok session B2 = slice 4 server half (categorizeAgentCutIds classifier + categorized suggest action). Two specialist reviewers (correctness/tests, api-contract/maintainability) running over the slice-1 diff in parallel.
- 2026-07-12 · Wave B landed and orchestrator-reviewed. B2 accepted (classifier + suggestCleanupCuts sibling action, +7 tests). B1 accepted (Cleanup tab after Project, category cards, bulk apply, undo-last; +21 tests; created missing Checkbox primitive). Full gates green under orchestrator: 1988 tests / 0 fail, typecheck, build.
- 2026-07-12 · Slice-1 reviewer verdicts: both REQUEST-CHANGES. api-contract reviewer found the blocker — addDeadAir coalescing means cleanup-apply can report a PRE-EXISTING span id as undoable, so Undo deletes an unrelated confirmed span (repro'd) — plus repeat×phrase double-claim (counts/savings inflation) and cuts.cleanup schema rejecting hand-edited out-of-range values at load (bricks project; contradicts captions.style .catch precedent). correctness reviewer corroborated both majors and added: cleanup-apply drops report warnings so MCP callers can't tell "no dead air" from "analysis missing"; enabled-mode apply is non-idempotent when repeat is on (post-cut adjacency reveals new repeats — ruled intentional iterative refinement, to be documented + convergence-tested); tryLoadSilencesSync has zero filesystem test coverage. Minors: category list duplicated ~6 places, CleanupCandidateCategory vs AgentCutCategories vocabulary collision (intentional, needs cross-refs), 50-span cap bypass (document), cleanup-config can't unset to inherit-default (accept null).
- 2026-07-12 · Fix lane FIX1 launched (grok, src/** + engine tests): 12-item contract = blocker created-vs-extended span ids, schema .catch degrade, warnings threading + async analysis load if registry supports it, phrase-loop dedupe, idempotency documentation + convergence test, provenance cleanup-apply case, MAX_NGRAM 3→6, category single-source constant, vocabulary cross-refs, cap-bypass comment, cleanup-config null-unset, features.ts 51→52 entry. In parallel: lane C1 (grok, web/** + silences route) = silences hydration, Remove-silence card with waveform strips + threshold sliders, bulk silence apply into undo snapshot, card stacking + tab-bar layout fixes.
- 2026-07-12 · FIX1 + C1 landed; orchestrator gates green (2015 tests / 0 fail, typecheck, build; 3 lint findings are pre-existing branch debt, queued for C2). features --json = 52. Browser round 2 on parity-demo: silence card matches the reference (stacked category cards with live counts incl. the 5-gram "I want to show you" repeat from the MAX_NGRAM fix; waveform strip with highlighted silence region + pad labels; threshold sliders; "4 silences · saves ~4.1s"); silences hydrate without snap (degraded warning gone); Apply-all-safe correctly grew to 6.
- 2026-07-12 · Orchestrator caught a cross-lane integration bug the lanes' tests can't see: dead-air-add now returns DeadAirTouch[] ({span, created}) but the GUI still casts flat spans — verified live: "Remove all silences" then Undo fires dead-air-rm with {} (undefined ids) and spans survive. Root cause of my earlier undo-count discrepancy risk; cleanup-apply's own undo path is unaffected (flat id arrays). Fix contracted to lane C2 along with the AI-pass button, pending hover highlight, and a better-ui polish pass.
- 2026-07-12 · Localhost verification on parity-demo (dev server :4399, browser-driven): Cleanup tab renders 3 category cards with counts/snippets; Hedging toggle persists sparse config to project.json ({categories:{hedging:true}}); "Apply enabled categories" cut exactly the 9 enabled-category words + registered all 4 dead-air spans server-side (duration 0:28→0:22); transcript strikethrough live; "Undo last cleanup (13)" fully restored (0 deleted words, 0 spans, duration 0:28). Wave-C findings: (a) GUI never hydrates silences unless snap is on — panel shows the degraded warning and no dead-air candidates even though the server has them; C1 must hydrate analysis for the tab (and the waveform needs it anyway); (b) category cards render 3-across in a ~240px sidebar — truncated snippets; stack them vertically; (c) config tab bar is cramped ("ProjectCleanup" collision).

## Ship (2026-07-13)

**Verdict: PASS** (Check mode, `$tstack-ship`, two consecutive passes — 2026-07-12 and 2026-07-13 — identical git state both times: HEAD `986e796`, 37 modified + 21 new files, +3,038/−229, no PR yet).

- Review gate: 2 specialist reviewers (correctness/tests; api-contract/maintainability) on the slice-1 engine diff, both initially REQUEST-CHANGES, all findings fixed with regression tests; plus a cross-model (codex/GPT-5.5) adversarial red-team on the full diff, verdict BLOCK with 1 blocker (stale AI-pass results applying to the wrong project after a slug switch — word ids are reused across projects), 2 major (peaks route unbounded window allocation; silences route cold inline analysis on long footage, accepted as consistent with existing architecture), 1 minor (loose param validation) — blocker and majors-requiring-fixes resolved with regression tests (FIX2 lane), gates re-verified green (2031 tests, up from 2022).
- QA gate: 3 rounds of live browser verification against a real project (`parity-demo`), including filesystem- and curl-level verification (not just component tests) of undo integrity and the hardened peaks route.
- CI: full local verification contract (`bun run check`, `bun run typecheck`, `bun test`, `bun run build`, `bun run agent-smoke-audit` — full parity with `.github/workflows/ci.yml`'s `test` job) run fresh at ship time, all green: 2026 pass / 5 skip / 0 fail (5 skips traced to source — 3 pre-existing OPENKLIP_INTEGRATION-gated browser tests, 2 from a zero-diff-against-main benchmark file gated on an absent local fixture; none introduced by this branch). Secrets scan of the diff clean.
- Missing coverage flagged, not blocking: no dedicated `$tstack-security-review`, `$tstack-performance`, or `$tstack-devex-qa` pass (relevant risk classes were covered by the general review/red-team rounds).
- Decision: ship as **one PR**, not the originally-planned four slice PRs. The two post-slice fix rounds (undo-integrity blocker, schema hardening, stale-AI-result race) touched files across multiple slices and were only ever tested against the full accumulated diff — retroactively splitting into four independently-gated PRs would mean shipping intermediate states never actually verified in isolation, and safely reconstructing them would require interactive rebase (disallowed). The PR body narrates all four slices and both fix rounds for reviewers.
