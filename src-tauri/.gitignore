/target
/gen/schemas
# Vendored per-build/per-target sidecar binary (CRAFT-6187 Stage B) — ~60MB,
# regenerated by the packaging build, never committed.
/binaries
# Generated runtime bundle (full .next + node_modules + assets), populated by
# scripts/prepare-desktop-bundle.ts before `tauri build`. Never committed.
/resources/app

# Updater signing key — SECRET, never commit
.tauri/
